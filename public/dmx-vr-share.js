export function createVRShare(host,{getScene,onCommand=()=>{}}){
  const root=document.createElement('details');root.className='stage-vr-share';root.innerHTML='<summary>VR-Vorschau verbinden</summary><p>Übertrage diesen Aufbau und die laufende Lichtshow. Musik bleibt am Rechner.</p><button type="button" class="button secondary" data-share-start>Übertragung starten</button><div data-share-links hidden><label>Im Headset öffnen<select aria-label="Vorschau-Adresse"></select></label><input type="text" readonly aria-label="Adresse für die VR-Vorschau"><button type="button" class="button secondary" data-share-copy>Adresse kopieren</button><p>Kopplungscode: <output data-share-code></output></p><p data-share-network></p><p><a data-share-test target="_blank" rel="noopener">Testzugang ohne Code öffnen</a></p></div><p role="status"></p>';
  host.append(root);const button=root.querySelector('[data-share-start]'),status=root.querySelector('[role=status]'),links=root.querySelector('[data-share-links]'),select=root.querySelector('select'),input=root.querySelector('input');
  let commandMessage='',ack=[];const applied=new Set();
  let session=null,sending=false,disposed=false,epoch=0,nextSend=0;
  async function api(action,body,keepalive=false){let token='';try{token=sessionStorage.getItem('wiz-web-token')||'';}catch{}const response=await fetch('/api/vr-preview/'+action,{method:'POST',headers:{'Content-Type':'application/json','X-AnyDj-Local':'1',...(token?{Authorization:'Bearer '+token}:{})},body:JSON.stringify(body),keepalive,signal:AbortSignal.timeout(4000)});const data=await response.json();if(!response.ok){const error=Error(data.error?.message||'Übertragung fehlgeschlagen.');error.status=response.status;throw error;}return data;}
  async function stop(){epoch++;const old=session;session=null;button.textContent='Übertragung starten';links.hidden=true;if(old)try{await api('stop',{id:old.id,owner:old.owner},true);}catch{}status.textContent='Übertragung beendet.';}
  button.onclick=async()=>{button.disabled=true;try{if(session)await stop();else{const own=++epoch;const result=await api('start',{});if(disposed||epoch!==own){await api('stop',{id:result.id,owner:result.owner},true);return;}session=result;ack=[];applied.clear();commandMessage='';nextSend=0;select.replaceChildren(...result.urls.map(url=>new Option(new URL(url).host,new URL(url).origin)));input.value=select.value;root.querySelector('[data-share-test]').href=select.value+'/vr-test';root.querySelector('[data-share-code]').textContent=result.code;links.hidden=false;button.textContent='Übertragung beenden';root.querySelector('[data-share-network]').textContent=result.secure?'Im gleichen Netzwerk öffnen. Das HTTPS-Zertifikat muss der Brille bekannt sein.':'Live-Vorschau über HTTP. Für „VR starten“ benötigt der Server HTTPS mit einem von der Brille akzeptierten Zertifikat.';status.textContent='Warte auf die Brille …';void publish();}}catch(error){status.textContent=error.message;}finally{if(!disposed)button.disabled=false;}};
  select.onchange=()=>{input.value=select.value;root.querySelector('[data-share-test]').href=select.value+'/vr-test';};
  root.querySelector('[data-share-copy]').onclick=async()=>{try{await navigator.clipboard.writeText(input.value);status.textContent='Adresse kopiert · in der Brille öffnen und Zahlencode eingeben.';}catch{input.focus();input.select();status.textContent='Markierte Adresse kopieren.';}};
  async function publish(){if(!session||sending||disposed||performance.now()<nextSend)return;sending=true;const current=session;try{const result=await api('frame',{id:current.id,owner:current.owner,scene:{...getScene(),controlMessage:commandMessage},ack});if(session!==current||disposed)return;for(const command of result.commands||[]){if(applied.has(command.id))continue;applied.add(command.id);try{await onCommand(command);commandMessage='';}catch(error){commandMessage=error.message;}ack.push(command.id);}ack=ack.slice(-100);if(applied.size>200){const keep=[...applied].slice(-100);applied.clear();keep.forEach(id=>applied.add(id));}nextSend=performance.now()+(result.viewers?0:450);if(session===current)status.textContent=result.viewers?`${result.viewers} Vorschau verbunden · Änderungen werden live übertragen.`:'Übertragung bereit · Adresse im Headset öffnen und Code eingeben.';}catch(error){nextSend=performance.now()+1000;if(session===current&&error.status===404){session=null;links.hidden=true;button.textContent='Übertragung starten';status.textContent='Vorschau abgelaufen. Bitte neu starten und den neuen Code eingeben.';}else if(session===current)status.textContent=`Verbindung unterbrochen: ${error.message} Erneuter Versuch läuft.`;}finally{sending=false;}}
  const timer=setInterval(publish,50);const unload=()=>{void stop();};window.addEventListener('pagehide',unload);
  return {get active(){return Boolean(session);},stop,destroy(){disposed=true;clearInterval(timer);window.removeEventListener('pagehide',unload);void stop();root.remove();}};
}
